# /data-raw

Drop raw downloaded data files here (e.g. NBIM's real holdings Excel/CSV) when you're ready to swap in real data — see `/scripts/README.md`.

This folder's contents are gitignored (raw downloads can be large and shouldn't be committed) except for this README, which just keeps the folder itself present in git.
